# Repository map

> Every tracked file, with its purpose and state. Kept current so a new session
> can locate work without reading the tree. **Update this in the same commit as
> any file added, moved, or deleted.**

**Last verified:** 2026-08-13 · Companion to [README.md](README.md)

**Legend:** ✅ implemented · 🚧 partial · 📁 empty project, structure only · ⬜ planned

---

## Root

| File | Purpose |
|---|---|
| `Brasa.slnx` | Solution. **.NET 10 XML format — not `.sln`** |
| `Directory.Build.props` | Solution-wide MSBuild. Nullable, `TreatWarningsAsErrors`, `InvariantGlobalization=false` (ICU needed for pt-PT and Azores) |
| `Directory.Packages.props` | Central package management. All versions live here; `.csproj` reference by name only |
| `.editorconfig` | Style + analyzer policy. Every suppression carries a written reason |
| `.gitattributes` | Line-ending normalisation. Fiscal fixtures marked `-text` (byte-exact) |
| `.gitignore` | Blocks keys, certs, `.env`, local DBs |
| `README.md` | Project front door |
| `CLAUDE.md` | Rules for Claude Code sessions |
| `CONTRIBUTING.md` | Workflow, commit format, documentation contract |
| `package.json` | **Docs site only** — VitePress. The .NET build does not use npm |

## `.github/`

| File | Purpose |
|---|---|
| `workflows/ci.yml` | Build (zero-warning gate) + test + transitive vulnerability scan + `e2e` job (Playwright, `src/web/e2e` — written, **not yet exercised by an actual CI run**; installs `pos` **and** `admin` dependencies now that `admin` (WEB-09) is a second app `playwright.config.ts`'s own `webServer` list starts — a fresh checkout would otherwise fail at that step, found while it still had no CI run to catch it) + `openapi-drift` job (API-14 — starts the real API, regenerates `docs/openapi/v1.json` the documented way, fails on any diff; not semantic breaking-change detection, just drift detection, and honestly labelled as such. Also **not yet exercised by an actual CI run** — validated locally instead by deliberately breaking a copy of the committed file and confirming the diff catches it) |
| `workflows/docs.yml` | Relative-link check across all markdown; warns when source changes without a `status.md` update |
| `workflows/pages.yml` | Builds the VitePress site from `docs/` and deploys to GitHub Pages |
| `pull_request_template.md` | Docs checklist, plus fiscal and money sections |
| `ISSUE_TEMPLATE/bug.md` | Bug report; severity includes a **Fiscal** tier |
| `ISSUE_TEMPLATE/feature.md` | Feature request; forces an offline-behaviour answer |
| `ISSUE_TEMPLATE/fiscal-change.md` | Fiscal change; requires citing the legal instrument |

## `src/backend/Brasa.Shared` — the shared kernel ✅

Depended on by every module; depends on no module. Deliberately small.

| File | State | Contents |
|---|---|---|
| `Primitives/Money.cs` | ✅ | Integer minor units. `Allocate` for splitting, `Format(culture)` for display, `ToString()` invariant. **The most important type in the system** |
| `Primitives/CurrencyCode.cs` | ✅ | `Eur = 0` so `default(Money)` is zero euros. Seam for future non-euro markets |
| `Primitives/Result.cs` | ✅ | `Result`, `Result<T>` with `Match` |
| `Primitives/Error.cs` | ✅ | `Error` + `ErrorType` → maps to HTTP status at the API edge. Six cases, not five, since API-12: `RateLimited` → 429 joined `Validation`/`NotFound`/`Conflict`/`Forbidden`/`Failure` |
| `Tenancy/ITenantContext.cs` | ✅ | Tenant / site / terminal / user for the current scope |
| `Tenancy/TenantContext.cs` | ✅ | Resolve-once-per-scope; throws on reassignment |
| `Time/IClock.cs` | ✅ | `IClock`, `SystemClock`. Nothing may call `DateTime.UtcNow` |
| `Time/TestableClock.cs` | ✅ | QA-04 — `IClock` with a per-request override in a static `AsyncLocal<DateTimeOffset?>`, not instance/DI-scoped state. `MockFiscalProvider` is deliberately singleton (its in-memory sequential numbering must survive across requests), and a singleton can't consume a scoped `IClock` — the `AsyncLocal` gives the same per-request isolation without constraining every consumer's own lifetime, the same reason `TenantContextAccessor` exists instead of a plain scoped `TenantContext`. `OverrideWith` is `static` (there is no instance state to mutate); only `Brasa.Api.Testing.TestClockMiddleware` ever calls it |
| `Time/PortugueseTimeZone.cs` | ✅ | `PortugueseRegion` (Continental/Madeira/Azores) + business-day calculation |
| `Persistence/Entity.cs` | ✅ | `IEntity`, `ITenantOwned`, `IAuditable`, `ISoftDeletable`, `Entity` (UUIDv7, `ValueGeneratedNever` — see [ADR 0010](../architecture/decisions/0010-rls-runtime-role-split.md) for why that matters) |
| `Persistence/TenantAwareDbContext.cs` | ✅ | Base `DbContext` every module extends. Applies query filters, stamps tenant/audit fields on save, guards against tenant reassignment and against hard-deleting an `ISoftDeletable` entity |
| `Persistence/RowLevelSecurity.cs` | ✅ | `EnableFor`/`DisableFor` — emits the real RLS policy **and** grants the runtime role access, in the same migration |
| `Persistence/TenantSessionInterceptor.cs` | ✅ | Sets the `brasa.tenant_id` Postgres session variable RLS policies read, per connection |
| `Persistence/ModelBuilderExtensions.cs` | ✅ | `ApplyTenantQueryFilters` (also excludes soft-deleted rows for any `ISoftDeletable` entity), `MapMoney`, `MapOptionalMoney` (CAT-06 — same two-column shape, for a `Money?` that's genuinely absent, not zero; `IsRequired(false)` on the complex property), `ApplyEntityConventions` (incl. the `ValueGeneratedNever` fix) |
| `Persistence/PersistenceServiceCollectionExtensions.cs` | ✅ | `AddBrasaTenancy()` — one call wires tenancy + the session interceptor for a host |
| `Tenancy/TenantContextAccessor.cs` | ✅ | `AsyncLocal`-backed singleton so EF's cached compiled model doesn't permanently capture one request's tenant |
| `Messaging/IntegrationEvent.cs` | ✅ | Event, handler and dispatcher contracts. **Only sanctioned cross-module channel** |
| `Messaging/OutboxMessage.cs` | ✅ | Transactional outbox row. Also the offline sync mechanism |

⬜ **Missing:** the dispatcher implementation.

## `src/backend/Brasa.Api` ✅ I0 + I1's first slice

| File | State | Contents |
|---|---|---|
| `Program.cs` | ✅ | Two-role DI wiring (runtime vs. migration connection — [ADR 0010](../architecture/decisions/0010-rls-runtime-role-split.md)), API versioning, CORS (`Cors:AllowedOrigins`, for web clients; `WithExposedHeaders("ETag", "X-Next-Cursor")` — API-10/API-09, neither is a CORS "simple" response header so browser JS can't read them without this), Brotli/gzip response compression incl. `application/problem+json` (API-11 — `UseResponseCompression()` must run first in the pipeline, before anything writes a response body), migration runner, dev seeding (Catalog + Floor), `ClientRequirements` config binding (API-07), `Api:Deprecation` config binding (API-08), `AddRateLimiter`/`RateLimiting` config binding with a custom `OnRejected` (API-12), full middleware pipeline incl. `ClientVersionMiddleware` (API-06, runs before request logging so enrichment reaches the request-completion line), `ApiDeprecationMiddleware` right after it, and `TenantLoggingMiddleware` (OPS-07) then `UseRateLimiter()` after `DevTenantMiddleware` but before `IdempotencyMiddleware` (rejected requests shouldn't consume an idempotency cache slot), `/health` liveness + `/health/ready` readiness (OPS-09), `GET /.well-known/apple-app-site-association`/`assetlinks.json` (API-18 — honestly empty `apps`/`details`/top-level arrays, unversioned, outside `/api/v1`). `GET /api/v1/ping` now takes `IClock` instead of calling `DateTimeOffset.UtcNow` directly — a real hard-rule-2 violation found while adding the two endpoints above it. OpenTelemetry wiring (OPS-08) — ASP.NET Core/`HttpClient`/Npgsql tracing (the last via `AddSource("Npgsql")`) plus ASP.NET Core/HTTP/runtime metrics, OTLP-exported to Seq when `Otel:OtlpEndpoint` is configured (empty by default); see the OTLP-endpoint-path trap in README.md before touching this. `AddSignalR()` + `MapHub<FloorHub>("/hubs/floor")` (API-16 — outside `/api/v1`, since a connection isn't a versioned resource). `IClock` now registers `TestableClock` (QA-04, singleton — see its own remarks for why not scoped) and `TestClockMiddleware` runs right after `DevTenantMiddleware`, same "throws if `IsProduction()`" shape. A second `app.UseStaticFiles(...)` mount (CAT-02) serves `MenuItemImageStorage.RootPath` at `/uploads/menu-items`, registered right before `app.UseRouting()` |
| `HealthChecks/DatabaseHealthCheck.cs` | ✅ | `IHealthCheck` running `SELECT 1` against the runtime (`brasa_app`) connection. Tagged `"ready"` — mapped only at `/health/ready`, never at the liveness `/health` — so PostgreSQL being briefly unreachable stops traffic routing but never triggers a process restart. OPS-09 |
| `Tenancy/DevTenantMiddleware.cs` | ✅ | Attributes every request to one hardcoded tenant. **The entire auth story until IDN-03…08 (I3).** Throws if `IsProduction()` |
| `Testing/TestClockMiddleware.cs` | ✅ | QA-04 — reads an optional `X-Brasa-Test-Clock` header (ISO 8601) and calls `TestableClock.OverrideWith` for the rest of this request only. Mirrors `DevTenantMiddleware` exactly: registered unconditionally in the pipeline, throws on the very first request if `IsProduction()`. A missing/unparseable header is silently ignored, not rejected — this is a testing lever, not public API surface |
| `Tenancy/TenantLoggingMiddleware.cs` | 🚧 | OPS-07 — pushes `TenantId`/`SiteId`/`TerminalId`/`UserId` onto Serilog's `LogContext` for the rest of the request; registered right after `DevTenantMiddleware`. Verified live (a real request's EF Core command log line carries `TenantId`) but doesn't reach `UseSerilogRequestLogging`'s own completion-summary line — that middleware runs earlier in the pipeline on purpose, see the class's own remarks for why moving it isn't a small change |
| `Idempotency/IdempotencyMiddleware.cs` | ✅ | Requires `Idempotency-Key` on mutating `/api` requests; replays the cached response on repeat. In-memory, per-instance — durable store needed before scaling out. Verified by an automated replay harness, not just the doc comment (QA-11, `idempotency.spec.ts`) |
| `ClientVersioning/ClientInfo.cs` | ✅ | `TryParse` for the `X-Brasa-Client` header (`<client-id>/<version> (<platform>)`, API-06) — never throws, since a missing/malformed header is the expected case today (no client sends it yet) |
| `ClientVersioning/ClientVersionMiddleware.cs` | ✅ | Best-effort header parse; stores the result on `HttpContext.Items` for `GET /client-requirements` and pushes `ClientId`/`ClientVersion`/`ClientPlatform` into Serilog's `LogContext` so every log line for the request carries it. Never fails the request on a missing/malformed header |
| `ClientVersioning/ClientRequirementEntry.cs` | ✅ | Configuration-bound version policy for one client id, under `appsettings.json`'s `ClientRequirements` section (API-07) — not the wire DTO, see `Contracts/ClientDtos.cs` |
| `ClientVersioning/ApiDeprecationOptions.cs`, `ApiDeprecationMiddleware.cs` | ✅ | API-08 — RFC 8594 `Deprecation`/`Sunset` headers on every response, from `Api:Deprecation` config (`DeprecatedAt`/`SunsetAt`/`Link`). Empty by default, so a no-op today — the same "ship the seam ahead of the trigger" shape as `ClientVersionMiddleware` itself: nothing to deprecate until a real `/api/v2` exists. Dates render as RFC 7231 IMF-fixdate (`.ToString("R", CultureInfo.InvariantCulture)` on the UTC instant — a non-UTC `DateTimeOffset` is converted first, not stamped "GMT" on its local wall-clock time) |
| `CursorPagination.cs` | ✅ | `Encode(DateTimeOffset)`/`TryDecode(string?, out DateTimeOffset)` (API-09) — opaque base64 bookmark token, applied so far only to `GET /orders` (the one genuinely unbounded collection today; `/menu` and `/floor` are both bounded by the restaurant's own size) |
| `ErrorMapping.cs` | ✅ | The only place `ErrorType` → HTTP status is decided — now six mappings, `RateLimited` → 429 (API-12) |
| `RateLimiting/RateLimitingOptions.cs`, `ApiRateLimiting.cs` | ✅ | API-12 — `ApiRateLimiting.ResolvePartitionKey` is pure, DI-free logic (unit-tested directly) deciding the `(tenantId, X-Brasa-Client client id)` bucket a request falls into, or `UnmeteredPartitionKey` outside `/api`; `Program.cs`'s `AddRateLimiter`/`UseRateLimiter` wire it into a real `PartitionedRateLimiter` with a custom `OnRejected` that writes `Error.RateLimited(...).ToProblem()` instead of the framework's bare 429 body. Coarser than the eventual goal: no terminal-level identity exists yet (IDN-03…08), so every `pos-web` client in a tenant shares one bucket |
| `MenuItemImageStorage.cs` | ✅ | CAT-02 — local-disk storage for menu item photos, an honest dev-only placeholder (no S3/Blob credentials in this environment, same reasoning as skipping OPS-11). `RootPath` under `{ContentRoot}/uploads/menu-items/` (gitignored); `Validate` checks size (5MB max) and content type (JPEG/PNG/WebP only) before `SaveAsync` writes a GUID-named file (`Guid.CreateVersion7()`, never the caller's own filename); `Delete` strips any directory component via `Path.GetFileName()` before rejoining under `RootPath`, so a maliciously-crafted `imageUrl` can only ever resolve to a file directly under the upload root. No per-tenant isolation — a named gap, not an oversight |
| `ETagResults.cs` | ✅ | `OkWithETag<T>(HttpContext, T)` (API-10) — SHA256 strong `ETag` over the serialized body, `304` when `If-None-Match` already matches. **Must** serialize with the app's configured `IOptions<JsonOptions>` (camelCase), not `JsonSerializer`'s own default (PascalCase) — using the wrong one silently renames every field in the response; this broke `pos`'s menu screen once, see the API-10 entry in [status.md](../product/status.md). Deliberately not used by `GET /floor` — table state changes too continuously for caching to pay off |
| `Hubs/FloorHub.cs` | ✅ | API-16/17 — this codebase's first realtime channel. `FloorHub : Hub`, broadcast-only, no server-invokable client methods; `FloorHubBroadcast.NotifyFloorChangedAsync(IHubContext<FloorHub>, CancellationToken)` sends one shared, payload-less `FloorChanged` event name so every call site can't typo a different string. Every connection sits in one `Clients.All` group — no per-tenant/per-terminal targeting yet, a named gap waiting on IDN-06/07 |
| `Endpoints/CatalogEndpoints.cs` | ✅ | `GET /menu` (`ETagResults.OkWithETag`, API-10, guest-facing — filters to visible categories/available items), `GET /menu/all` (WEB-10 — the same data unfiltered, `admin`'s management view; a hidden category or 86'd item has to be *visible* to the screen that turns it back on), `DELETE /menu/items/{id}` (CAT-18, soft delete), `PUT /menu/items/{id}/details` (CAT-02 — description + declared allergens, the 14-category EU-regulated set), `POST`/`DELETE /menu/items/{id}/image` (CAT-02 — uploads/replaces or removes a photo via `MenuItemImageStorage`, local disk; `.DisableAntiforgery()` on the upload route, since ASP.NET Core auto-attaches antiforgery metadata to any endpoint binding an `IFormFile` even though this app registers no antiforgery middleware at all; saves the new file and persists the new `ImageUrl` before ever deleting whichever file it replaces), `POST /menu/items/import` (CAT-17, CSV) and `POST /menu/items/import/excel` (CAT-17, `.xlsx` — `ExcelImportParser`) — both funnel into one shared `ImportRowsAsync` once each has turned its file into the same row shape, rows import independently, `400 catalog.import_empty`/`catalog.import_invalid_header`/`catalog.import_invalid_file` for the whole-file failure cases; also the only way to *create* a menu item at all, admin included), `PUT /menu/items/{id}/availability` (CAT-13 — `MarkAvailable`/`MarkUnavailable` and the `AddLine` guard for both existed since I0; this is the endpoint that finally makes `IsAvailable` reachable), `PUT /menu/items/{id}/price` (CAT-19 — same shape: `MenuItem.Reprice` and its negative-price guard existed since I0 with no caller; safe by construction since `OrderLine.UnitPrice` already snapshots at add-time), `PUT /menu/items/{id}/takeaway-price` (CAT-06 — sets/clears `MenuItem.TakeawayPrice`; null means "same as dine-in," the same convention as `Course`/`Station`; `AddLineAsync` picks it over `Price` when `Order.IsTakeaway`, VAT rate untouched either way — that's `TaxRule`'s job, CAT-07/08, not this endpoint's), `PUT /menu/items/{id}/course` (CAT-14 — a greenfield addition, not a dormant-method gap like CAT-13/19: sets/clears which point in the meal an item is served at, independent of `MenuCategory`; the value it sets is what `OrderLine.Course` later snapshots for course firing, ORD-07/08/09), `PUT /menu/items/{id}/station` (CAT-15 — same greenfield shape as CAT-14: which kitchen station prepares an item, independent of both `MenuCategory` and `Course`; ships ahead of station *routing*, KIT-06, not built yet), `PUT /menu/items/{id}/schedule` (CAT-11 — sets/clears `MenuItem.Schedule`, a recurring day/time window for a *prato do dia*; all-or-nothing, days+start+end together or all three cleared, `400 catalog.incomplete_schedule` otherwise; `GetMenuAsync` now filters a scheduled item out entirely outside its own window, resolved via mainland `Europe/Lisbon` time — no per-tenant region exists yet to pick a real one — while `GetMenuAllAsync` never filters on it, same split as CAT-01/13), `PUT /menu/items/{id}/couvert` (CAT-12 — sets/clears `MenuItem.IsCouvert`, a plain tag that never filters `GET /menu` either — `AddLine` needs no changes at all, adding couvert is the same call as any other line; `pos`'s dedicated `CouvertBar` is the only thing that reads this flag today), `PUT /menu/items/{id}/scheduled-price` (CAT-16 — sets/clears a pending future price; every `ToDto()` call site in this file passes its own injected `IClock.UtcNow` so `MenuItemDto.Price` is always `MenuItem.EffectivePrice`, never the stale raw value, whether shown by `GetMenuAsync`, `GetMenuAllAsync`, or any other mutation's own echoed response), `PUT /menu/categories/{id}/visibility` (CAT-01 — one level up from the other three: `MenuCategory.IsVisible` had no setter *at all*, not even an unreachable one; hides the category and every item under it from `GET /menu` in one call). Every mutation here is called for real now — `admin`'s menu editor (WEB-10) |
| `Csv/CsvParser.cs` | ✅ | Hand-written RFC 4180 reader (CAT-17) — quoting, `""`-escaped quotes, embedded newlines, CRLF/LF, blank lines skipped without producing an empty row. 8 unit tests in `tests/Brasa.Api.IntegrationTests/CsvParserTests.cs` |
| `Csv/ExcelImportParser.cs` | ✅ | CAT-17's Excel half — reads the first worksheet of an uploaded `.xlsx` via `ExcelDataReader` into the exact row shape `CsvParser` produces, so `CatalogEndpoints` runs both through one shared pipeline; blank rows skipped the same way. `ExcelDataReader` chosen over ClosedXML/NPOI specifically for its minimal, vulnerability-free dependency footprint (read-only, no need to write Excel). Static constructor registers `CodePagesEncodingProvider` — `.xlsx` needs it too, not just legacy `.xls`, a real gotcha found live (see the trap in README.md) |
| `Endpoints/FloorEndpoints.cs` | ✅ | `GET /floor`, `POST /tables/{id}/clear`, `POST /tables/{id}/request-bill` (FLR-04 — mirrors `ClearTableAsync` exactly, incl. the `DbUpdateConcurrencyException` → same-guard-error handling for the `xmin` race), `POST /rooms/{id}/tables`/`PUT /tables/{id}`/`DELETE /tables/{id}` (FLR-03 table CRUD, not the drag-and-drop canvas that row's own title names; `DeleteTableAsync` requires `Free`, `UpdateTableAsync`'s concurrency race is a genuine lost-update, not a state re-check, so it gets its own code — `floor.table_concurrently_modified` — rather than reusing one of the state-specific ones), `POST /rooms`/`PUT /rooms/{id}`/`DELETE /rooms/{id}` (FLR-03's room-CRUD follow-up — `DeleteRoomAsync` checks `db.Tables.AnyAsync` directly, since `Room` has no navigation to `Table` to check from the domain side; both `POST`/`PUT` also carry `FloorLevel` now, FLR-07), `POST /table-groups`/`DELETE /table-groups/{groupId}` (FLR-05 — pushes 2+ distinct, all-`Free` tables into one seating unit via `Table.JoinGroup`/`LeaveGroup`; each table's own domain call is checked before any `SaveChangesAsync`, so a mid-loop failure persists nothing), `PUT /rooms/{id}/section` (FLR-06 — `AssignRoomSectionAsync` composes `IdentityDbContext` in to confirm a non-null `staffId` names a real `Staff` row before calling `Room.AssignSection`; any role works, not manager-only, unlike IDN-11's gate). `ClearTableAsync`/`RequestBillAsync` both broadcast `FloorHub`'s `FloorChanged` signal (API-16) right after their own successful save. `GetFloorAsync`/`UpdateRoomAsync`/`AssignRoomSectionAsync` all compose `IdentityDbContext` too now, via the shared `ResolveStaffNamesAsync` helper — one batched query per call resolving every distinct `AssignedStaffId` across however many rooms are being mapped, not N+1, so `RoomDto.AssignedStaffName` is always resolved fresh from Identity, never snapshotted onto `Room` itself |
| `Endpoints/IdentityEndpoints.cs` | ✅ | IDN-01, a narrow first slice — `POST`/`GET /organizations`, `POST`/`GET /organizations/{id}/sites`, `POST`/`GET /sites/{id}/terminals`. Create + list only, no update/delete, no pairing/auth (IDN-06/07 stay untouched). `CreateSiteAsync`/`ParseRegion` reject an unrecognised `PortugueseRegion` name the same shape `ParseTableShape` (`FloorEndpoints.cs`) already established for `TableShape`. `POST`/`GET /sites/{id}/staff`, `POST /staff/{id}/verify-pin`, `PUT /staff/{id}/pin` (IDN-08/09) — `ParseRole` mirrors `ParseRegion`'s own shape for `StaffRole`; `VerifyStaffPinAsync` always calls `SaveChangesAsync` regardless of `VerifyPin`'s own outcome, since a *failed* attempt still needs its `FailedPinAttempts` increment (and possible lockout) persisted, not just a success. `Staff.VerifyPin` gets a second caller now too (IDN-11) — see `OrderEndpoints.AuthorizeManagerAsync` below |
| `Endpoints/PriceListEndpoints.cs` | ✅ | CAT-05, unblocked by IDN-01's `Site` — `POST`/`GET /price-lists`, `GET /sites/{id}/price-lists`, `POST /price-lists/{id}/entries`, `GET /price-lists/{id}/effective-price/{menuItemId}` (the actual override-or-fallback resolution, not just storage). `CreatePriceListAsync`/`GetPriceListsForSiteAsync` inject both `CatalogDbContext` and `IdentityDbContext` to confirm a `siteId` is real — composition sanctioned at the API layer, the same shape `OrderEndpoints` already uses across four modules. Create/read/add-entry only, nothing in `AddLine` or either web client resolves an effective price through this yet — no site-selection concept exists in `pos`/`admin` today |
| `Endpoints/ComboEndpoints.cs` | ✅ | CAT-10 — `POST`/`GET /combos`, `GET /combos/{id}`, `POST /combos/{id}/components`. Create/read/add-component only. Ringing a combo up is deliberately not here — `AddComboLineAsync` (`OrderEndpoints.cs`) is the piece that actually reaches `AddLine`, since it needs Ordering too |
| `Endpoints/TaxRuleEndpoints.cs` | ✅ | CAT-07/08 — `POST /tax-rules` (create + list only, no update/delete — a correction is a new, later-effective row, never an edit), `GET /tax-rules/resolve?isAlcoholic=&isTakeaway=&region=&atUtc=` (`TaxRule.Resolve` picks the rule in force; `atUtc` defaults to `IClock.UtcNow`; `404 catalog.tax_rule_not_found` when nothing covers the combination). Not yet wired into `AddLine`/`AddComboLineAsync`/the fiscal document builder — those still read `MenuItem.VatRate` directly, deliberately, since rewiring the live VAT path is its own fiscally-sensitive task |
| `Endpoints/OrderEndpoints.cs` | ✅ | `POST /orders` (against a real `tableId`; also resolves + validates `selectedModifierIds` — `ResolveModifiers`; both this and `POST /orders/{id}/combo-lines` resolve `MenuItem.EffectivePrice(clock.UtcNow)` — CAT-16 — before snapshotting a price or weighting a combo's allocation, never the raw stored `Price`, so a guest is charged exactly what `GET /menu` just showed), `POST /orders/{id}/combo-lines` (CAT-10 — rings a `Combo` (Catalog) up as one ordinary `AddLine` call per component, price allocated via `Money.Allocate` weighted by each component's own standalone price, VAT rate untouched per component — the same proration ORD-11 already uses for an order-level discount, so a combo mixing VAT bands itemizes correctly by construction, never a new fiscal concept), `POST /orders/takeaway` (ORD-20 — no Floor involvement at all), `GET /orders` (history/search — ORD-22, filters + capped `take` + cursor pagination via `X-Next-Cursor` — API-09), `GET /orders/{id}`, `POST /orders/{id}/lines`, `PUT /orders/{id}/lines/{lineId}/notes` (ORD-06), `PUT /orders/{id}/lines/{lineId}/discount` + `PUT /orders/{id}/discount` (ORD-11 — line and order-level percentage/fixed discounts; `BuildFiscalLines` is the shared helper `GET /orders/{id}/pre-bill` and `POST /orders/{id}/close` both call, folding a discount in as its own negative `FiscalDocumentLine` — an order-level one prorated across lines via `Money.Allocate` — so `order.Total` and `document.GrossTotal` reconcile with a discount applied the same way they already did without one), `POST /orders/{id}/lines/{lineId}/void` (ORD-10 — cancels a rung-up line with a required reason; `BuildFiscalLines` skips a voided line entirely rather than rendering it as a 100%-discount, since it was never delivered; voiding *every* line on an order still passes `Close()`'s own line-count guard but then correctly fails at `IFiscalProvider`'s pre-existing `fiscal.no_lines` check instead, leaving the order genuinely still `Open`), `POST /orders/{id}/fire` (ORD-07/08/09 — `FireOrderLinesAsync`: validates `request.Course` against Catalog's own `Course` enum names before ever calling `Order.FireLines`, so a typo 400s instead of silently firing zero lines; no manager authorisation, unlike void/discounts — firing touches no money). All three void/discount endpoints now gated by `AuthorizeManagerAsync` (IDN-11) — composes `IdentityDbContext` in (the class doc comment's own module list grew to include Identity), resolves `request.ManagerStaffId`, rejects a real but non-`Manager` staff id with `Error.Forbidden("identity.staff_not_manager", ...)` *before* ever calling `Staff.VerifyPin` (so a Staff-role credential never spends its own lockout budget), then reuses `VerifyPin` exactly as `IdentityEndpoints.VerifyStaffPinAsync` does — same lockout persistence regardless of outcome. Called right after the order lookup, before any of the three endpoints' own domain method — `Order.VoidLine`/`SetLineDiscount`/`SetDiscount` (`Modules.Ordering/Domain/Order.cs`) all stayed exactly as they were, since authorisation is layered on entirely at the API level), `POST /orders/{id}/lines/{lineId}/transfer` (ORD-13 — moves a line onto a different open order, pure Ordering, no Floor involvement), `POST /orders/{id}/merge` (ORD-14 — moves every line from a secondary order into the primary, marks the secondary `Merged`, frees its table via `Release()`), `POST /orders/{id}/transfer` (ORD-12 — order status checked before either table is touched, then both table mutations commit in one `FloorDbContext` save), `GET /orders/{id}/split`, `POST /orders/{id}/split/by-item` (ORD-16 — structured body, still a pure preview; now line-discount- and void-aware too — each line's own `LineTotal` is allocated across its quantity via `Money.Allocate` before being handed to whichever groups claim those units, rather than the raw unit price; remaining known gap — an *order-level* discount still isn't prorated in here, unlike `GET /orders/{id}/split` and `/split/by-cover`, which inherit it automatically through `Order.Total` — deliberately left open pending a real answer to "how do you fairly split 10% off the table between two guests who ordered different things?"), `GET /orders/{id}/split/by-cover` (ORD-17 — reuses `Money.Allocate(ReadOnlySpan<int>)` directly), `GET /orders/{id}/pre-bill` (ORD-18/19 — never calls `IFiscalProvider`), `POST /orders/{id}/close`. Composes Catalog + Ordering + Floor + Fiscal — see the module-boundaries note below. `OpenOrderAsync`/`TransferOrderAsync`/`MergeOrdersAsync`/`CloseOrderAsync` also broadcast `FloorHub`'s `FloorChanged` signal (API-16) after any table-state change they make actually commits |
| `Endpoints/ClientEndpoints.cs` | ✅ | `GET /client-requirements` (API-07) — reads the `ClientInfo` `ClientVersionMiddleware` parsed off `HttpContext.Items`, looks it up in the config-bound `ClientRequirements` section, `400 client.header_required` if absent/malformed, `404 client.unknown_client_id` if the client id isn't configured |
| `Contracts/*.cs` | ✅ | `MoneyDto`, `MenuItemDto` (incl. `Description`/`Allergens`, CAT-02; `ImageUrl`, CAT-02 — relative to the API's own origin, not `/api/v1`; `Course`, CAT-14; `Station`, CAT-15)/`MenuCategoryDto`/`ModifierGroupDto`/`ModifierDto`/`UpdateMenuItemDetailsRequest` (CAT-02)/`UpdateMenuItemCourseRequest` (CAT-14)/`UpdateMenuItemStationRequest` (CAT-15), `RoomDto`/`TableDto`, `OrderDto`/`OrderLineDto` (both incl. `DiscountType`/`DiscountValue`/`DiscountAmount`, ORD-11; `OrderDto` also `IsTakeaway`, ORD-20; `OrderLineDto` also `Notes`, ORD-06, `IsVoided`/`VoidReason`, ORD-10, and `Course`/`IsFired`/`FiredAtUtc`, ORD-07/08/09)/`OrderLineModifierDto`/`SetLineNotesRequest`/`FireOrderLinesRequest` (ORD-07/08 — a `Course` name to fire just that course, or null to fire everything unfired)/`SetDiscountRequest` (ORD-11 — shared by both discount endpoints, now also carrying `ManagerStaffId`/`ManagerPin`, IDN-11)/`VoidLineRequest` (ORD-10, same two new fields)/`TransferOrderRequest` (ORD-12)/`TransferLineRequest`+`TransferLineResponse` (ORD-13)/`MergeOrdersRequest`+`MergeOrdersResponse` (ORD-14)/`SplitByItemRequest`+`SplitByItemResponse` (ORD-16)/`OpenTakeawayOrderRequest` (ORD-20), `OrderSummaryDto` (ORD-22 — lighter than `OrderDto`, no line detail), `FiscalDocumentDto`, `PreBillDto`/`VatBreakdownDto` (ORD-18/19 — deliberately shaped nothing like `FiscalDocumentDto`), `ClientRequirementsDto` (API-07), `ImportMenuItemsRequest`/`ImportMenuItemsRowError`/`ImportMenuItemsResponse` (CAT-17), `UpdateMenuItemAvailabilityRequest` (CAT-13), `UpdateMenuItemPriceRequest` (CAT-19), `UpdateMenuCategoryVisibilityRequest`/`MenuCategoryVisibilityDto` (CAT-01) + mappings; `IdentityDtos.cs` — `OrganizationDto`/`SiteDto`/`TerminalDto` + `Create*Request` for each (IDN-01); `StaffDto`/`CreateStaffRequest`/`StaffPinRequest` (IDN-08/09 — `StaffDto.IsLocked` computed server-side from `LockedUntilUtc` + the caller's own injected `IClock`, so a client never needs to compare a raw timestamp against its own clock); `PriceListDtos.cs` — `PriceListDto`/`PriceListEntryDto`/`EffectivePriceDto`/`CreatePriceListRequest`/`AddPriceListEntryRequest` (CAT-05); `ComboDtos.cs` — `ComboDto`/`ComboComponentDto`/`CreateComboRequest`/`AddComboComponentRequest`/`AddComboLineRequest` (CAT-10); `MenuItemDto` also carries `ScheduledPriceDto?`/`SetScheduledPriceRequest` (CAT-16); `TableDto` also carries `GroupId`/`CreateTableGroupRequest` (FLR-05); `TaxRuleDtos.cs` — `TaxRuleDto`/`CreateTaxRuleRequest`/`ResolvedTaxRuleDto` (CAT-07/08); `RoomDto`/`CreateRoomRequest`/`UpdateRoomRequest` also carry `FloorLevel` (FLR-07); `RoomDto` also carries `AssignedStaffId`/`AssignedStaffName` and a new `AssignRoomSectionRequest` (FLR-06 — the name is resolved fresh by `FloorEndpoints`, never a field on `Room` itself) |

**API-15's second slice (response schemas):** every `.MapGet`/`.MapPost`/
`.MapPut`/`.MapDelete` across all eight endpoint files above now chains an
explicit `.Produces<T>(statusCode)` after its `.WithSummary(...)` — 68
route mappings in total, naming exactly the DTO/status each handler
already returned (`OrderDto` at 200, `Results.Created(...)` responses at
201, `Results.NoContent()` at 204, `ETagResults.OkWithETag`'s two
possible outcomes as 200 *and* 304 separately). Nothing about any
handler's own logic changed — this is pure OpenAPI metadata, additive and
side-effect-free, closing the gap `docs/openapi/README.md` names. Error
responses (`400`/`404`/etc.) still aren't described — a separate,
materially larger task.
| `Seed/DevCatalogSeeder.cs` | ✅ | Seeds a Portuguese demo menu spanning both VAT bands, plus two items with modifier groups (Frango na Brasa's required "Tamanho", Água's required "Tipo") and a couple with a description/declared allergens (Bacalhau à Brás, Pastel de Nata — CAT-02). Guarded the same way as the mock fiscal provider |
| `Seed/DevFloorSeeder.cs` | ✅ | Seeds 2 rooms / 16 tables (doubled from 8 once the E2E suite passed twenty tests and started occasionally exhausting the pool under back-to-back full runs — see e2e-testing.md). Same guard |
| `Seed/DevIdentitySeeder.cs` | ✅ | IDN-01 — seeds one full chain: "Brasa Demo, Lda" (organization) → "Restaurante Central" (site, `PortugueseRegion.Continental`) → "Caixa 1" (terminal). Same guard. Staff seeding (IDN-08/09, two rows — one Manager, one Staff) is a separate, independently-idempotent step that runs regardless of whether the org/site above was just created or already existed — a step gated behind the *old* "no organizations yet" early return would have silently never run on this session's own long-lived dev database, the exact trap CAT-07/08's tax-rule seeding hit once already (see the trap in README.md) |
| `appsettings.json` | ✅ | **Two** connection strings — `Postgres` (runtime, `brasa_app`) and `PostgresMigrations` (`brasa`, superuser); `ClientRequirements` section keyed by client id (API-07) — `pos-web` is the only real entry, add a row when `kds`/`admin`/`order` start sending `X-Brasa-Client`; `RateLimiting` (API-12) — deliberately generous (1000/60s) since every client shares one bucket per tenant until per-terminal keying exists. No `Otel:OtlpEndpoint` here on purpose (OPS-08) — no real OTLP collector exists for a production deployment yet |
| `appsettings.Development.json` | ✅ | CORS origins for `pos`/`admin`; a much higher `RateLimiting` override (10000/60s) than the base default — every dev/E2E request today shares the same `unknown`-client bucket per tenant, and the base default is tuned as a production backstop, not to the Playwright suite's own request volume; `Otel:OtlpEndpoint` (OPS-08) points at the local Seq instance's OTLP ingestion base — `Program.cs` appends the per-signal path itself |

## `src/backend/Brasa.Modules.*`

| Module | State | Roadmap |
|---|---|---|
| `Identity` | 🚧 IDN-01, IDN-08/09 | `Organization` (tenant-scoped, not assumed 1:1 with `TenantId`), `Site` (`OrganizationId`, `Name`, a real `PortugueseRegion` from day one), `Terminal` (`SiteId`, `Label` — a bare registry row, no pairing/auth yet), `Staff` (`SiteId`, `Name`, `StaffRole` — Staff/Manager — `PinHash` private, `FailedPinAttempts`, `LockedUntilUtc`; `VerifyPin`/`SetPin` domain methods, 5-strike/15-minute lockout), `PinHasher` (internal — PBKDF2-HMAC-SHA256 via `Rfc2898DeriveBytes`, `CryptographicOperations.FixedTimeEquals` for verification); EF config + migration (RLS) + design-time factory. `Staff.PinHash` is mapped via `builder.Property<string>("PinHash")` — string-keyed shadow-style access works for a genuinely `private` auto-property too, not just true EF shadow properties, so nothing outside `Staff` can read a hash back out. OAuth/JWT sessions, terminal pairing, roles-and-permissions beyond a bare Staff/Manager tag — the rest of this epic — still I3 |
| `Catalog` | ✅ `MenuCategory`, `MenuItem` (soft-deletable, CAT-18; `TakeawayPrice` — CAT-06, nullable, "same as dine-in" when unset), `ModifierGroup`/`Modifier` (CAT-03/04, one item per group for now), `VatRate` (the flat rate `MenuItem` still carries live — `TaxRule`, CAT-07/08, exists now but isn't wired into ordering yet, see that type's own remarks), `Course` (CAT-14 — which point in the meal an item is served at, independent of `MenuCategory`), `KitchenStation` (CAT-15 — which station prepares it, independent of both), `MenuItemSchedule` (CAT-11 — recurring day/time window), `PriceList`/`PriceListEntry` (CAT-05 — per-site price overrides, `SiteId` a plain opaque reference to Identity's `Site`, same pattern `Order.TableId` uses for Floor; owns its entries the same way `MenuItem` owns `ModifierGroup`), `Combo`/`ComboComponent` (CAT-10 — fixed-price bundles, always exactly one unit per component; holds no fiscal logic of its own, see `OrderEndpoints.AddComboLineAsync`), `ScheduledPriceChange` (CAT-16 — a pending future price change; persisted as two flat fields on `MenuItem`, not nested — EF Core cannot constructor-bind a complex type nested inside another, see the trap in [README.md](README.md)), `TaxRule` (CAT-07/08 — effective-dated VAT rate keyed by alcohol band × channel × region, `TaxRule.Resolve` picks the rule in force at an instant), EF config + migrations + design-time factory | — |
| `Ordering` | ✅ `Order` aggregate (opens against a real `TableId`), `OrderLine` (incl. `IsVoided`/`VoidReason`/`VoidedAtUtc`, ORD-10; `Course` — a plain string snapshotted from Catalog's `MenuItem.Course` at add-time, same convention as `ItemName`, ORD-07; `IsFired`/`FiredAtUtc`, ORD-08/09), `OrderLineModifier` (snapshotted selections), `SelectedModifier` (the record the API layer hands in — Ordering never resolves a modifier id itself), `OrderStatus`, `DiscountType` (ORD-11 — line/order discount kind; both `Order` and `OrderLine` expose it as `DiscountKind`, not `DiscountType`, so the property doesn't shadow the enum's own name inside those classes); `Order.FireLines(course, firedAtUtc)` (ORD-07/08 — a course name fires only its own unfired lines, null fires everything still pending; idempotent, skips a line already fired); EF config + migrations + design-time factory | — |
| `Floor` | ✅ `Room` (`Update` — FLR-03's room-CRUD follow-up, renaming/reordering; `FloorLevel: int`, default `0` — FLR-07, which physical storey it sits on, a display concern only), `Table` (`Free`/`Occupied`/`BillRequested`/`Dirty` state machine, `xmin` optimistic concurrency — see the trap in [README.md](README.md); `Update`/`EnsureCanDelete` — FLR-03's table CRUD, editing/removing a table; `GroupId`/`JoinGroup`/`LeaveGroup` — FLR-05, pushes free tables into one seating unit; `Occupy()`/`EnsureCanDelete` both refuse a grouped table so grouping has real teeth, not just a cosmetic tag); EF config + migrations (RLS) + design-time factory | — |
| `Fiscal` | ✅ `IFiscalProvider`, `FiscalDocument`, `FiscalDocumentLine` (VAT-inclusive derivation), `FiscalDocumentRequest`, `FiscalDocumentType` | — |
| `Payments` | 📁 empty | I6 |
| `Reporting` | 📁 empty | I8 |

Each references **only** `Brasa.Shared`. Never each other. `Order.TableId` is
a plain `Guid` reference to a Floor table, the same pattern
`OrderLine.MenuItemId` uses for a Catalog item — never a live query across
modules. `Brasa.Api`'s `OrderEndpoints` composes Catalog + Ordering + Floor +
Fiscal in one handler — that composition is the API layer's job, not
something the modules do to each other. See
[module-boundaries.md](../architecture/module-boundaries.md).

## `src/backend/Brasa.Fiscal.*`

| Project | State | Notes |
|---|---|---|
| `Fiscal.Portugal` | 📁 | ATCUD, RSA chain, QR, SAF-T, AT webservices. **The certification subject.** I7 |
| `Fiscal.Mock` | ✅ | `MockFiscalProvider` — deterministic, every value `MOCK-`-prefixed. `FiscalMockServiceCollectionExtensions` throws if registered under `IsProduction()` |

## `src/agent/Brasa.SiteAgent` 🚧

| File | State | Contents |
|---|---|---|
| `Program.cs` | ✅ | Generic host bootstrap |
| `Worker.cs` | 🚧 | Starts and stops cleanly. Documents the four planned responsibilities and nothing more |

⬜ **Missing (Month 3):** SQLite store, fiscal signing, ESC/POS printing, LAN
REST + SignalR hub, cloud outbox sync.

## `src/web/ui` ✅ WEB-02 — shared source for `pos`/`admin`

No build step and no `node_modules` of its own beyond what the workspace
root hoists in. Consumed by a Vite `resolve.alias` (`@brasa/ui` →
`../ui/src`) plus a matching TypeScript `paths` entry in each app's own
`vite.config.ts`/`tsconfig.app.json` — the alias treats these files as
that app's own source, never as an installed `node_modules` package (which
would hand Vite's dependency pre-bundler raw TSX and break). `src/web/`
itself gained a workspace-root `package.json` (`"workspaces": ["pos",
"admin", "e2e", "ui"]`) purely to hoist `react`/`react-i18next` to a
shared ancestor `node_modules` — the alias alone resolves this package's
*own* files, not its own bare imports, which still need a real
`node_modules` reachable by walking up from `ui/src/`.

| File | State | Contents |
|---|---|---|
| `package.json` | ✅ | `react`/`react-i18next` as `peerDependencies`, not `dependencies` — supplied by whichever app imports this package, never installed twice |
| `src/lib/money.ts` | ✅ | `formatMoney`, moved from `pos`/`admin`'s own duplicate copies. Takes a locally-defined `MoneyLike` structural type (`{ amount, currency }`), not either app's own `MoneyDto` import — TypeScript's structural typing makes both apps' DTOs compatible with no cast |
| `src/i18n/languageStorage.ts` | ✅ | `SupportedLanguage` type + `cookieLanguageStore`, moved from `pos`/`admin`'s own duplicate copies. Same `brasa.lang` cookie both apps already shared on purpose (ADR 0011) — now one implementation instead of two |
| `src/components/LanguageToggle.tsx` | ✅ | The pt/en toggle button group, moved from `pos`/`admin`'s own duplicate copies |

⬜ **Deliberately not shared:** `errorReporting.ts`/`ErrorFallback.tsx`/
`DevCrashTrigger.tsx` (OPS-14) and `i18n.ts` itself stay per-app — Sentry
init and each app's own translation resources differ enough that sharing
them would trade real duplication for a worse abstraction.

## `src/web/sdk` 🚧 API-15 — both request and response types, no consumer yet

`npm run generate` runs `openapi-typescript` against the *committed*
`docs/openapi/v1.json` (never a live API — same manual-regeneration
discipline that file already has) to produce `src/schema.ts`. Shipped in
two slices the same session: building the first surfaced that `v1.json`
had drifted (this session's own IDN-11/FLR-06 work hadn't regenerated it)
and, more consequentially, that every response body in it had always been
undescribed since API-13 first shipped — every endpoint returns a bare
`Results.Ok(...)`/`IResult`, which erases the concrete type OpenAPI
reflection needs to describe a response (it can still infer a request
body from a strongly-typed parameter). The second slice closed that: all
68 route mappings across eight endpoint files now carry an explicit
`.Produces<T>(statusCode)` call, so both halves of the contract are
described now — see [docs/openapi/README.md](../openapi/README.md)'s own
update. Error responses (`400`/`404`/etc.) still aren't — a separate,
materially larger task. No `pos`/`admin` call site consumes this yet, and
there's no typed fetch client wrapping it either — see the package's own
README for exactly why both are deliberately unstarted.

| File | State | Contents |
|---|---|---|
| `package.json` | ✅ | `openapi-typescript` as a `devDependency` (installed with `--legacy-peer-deps` — its peer range is still `typescript@^5.x`, this repo is on `~6.0.2`; a codegen-time-only tool, verified after the fact by typechecking its own output with the real TS 6 compiler, not by trusting the peer range) |
| `src/schema.ts` | ✅ | Generated, committed (same "reviewable in a diff" reasoning `docs/openapi/v1.json` itself already has) — `paths`/`components["schemas"]` typed from every request body, path and query parameter, *and* now every success response body too; `content?: never` remains only on genuinely bodyless `204`/`304` statuses and the out-of-scope `/ping` endpoint |
| `src/schema.guard.ts` | ✅ | A permanent type-level check, not a throwaway — literal objects built against `components['schemas']['VoidLineRequest']`/`AssignRoomSectionRequest` (request shapes) and `operations['GetFloor']['responses'][200]['content']` (a response shape, added the same session response types landed), incl. one deliberately-wrong field behind `@ts-expect-error` (so the guard itself fails loudly if it ever stops catching anything). Checked by `npm run typecheck` here, the same doc-drift trap class this codebase has hit repeatedly |

## `src/web/pos` ✅ I0 + I1's first slice

React 19 + Vite 8 + TypeScript. No auth, no offline — proves the API in a
browser. Hand-written API layer (`src/api/`) is a placeholder for `web/sdk`
(WEB-03/API-15, generated from OpenAPI) — the generator exists now, but
nothing here has switched over to it yet.

| File | State | Contents |
|---|---|---|
| `src/App.tsx` | ✅ | Orchestrates the phases: table picker (floor) → order (menu + summary) → receipt. `describeError()` looks up `error.code.<code>` in `resources/{pt,en}.ts` before falling back to the server's raw (always-English) `ProblemDetails` message — closes the "Server-sent error text" gap ADR 0011 named, for the ~20 codes `pos`'s own API calls can trigger. Connects `FloorHub` (API-16) once on mount via `connectFloorHub`, re-running `loadFloor()` on every push so another terminal's table change shows up live. Resolves a `siteId` (WEB-07, same "first organization's first site" shortcut `admin` already takes) and the site's staff list, both passed down to `StaffLogin` — a genuine failure to resolve either just means no login is offered, not a broken app, since nothing else here depends on it |
| `src/api/client.ts` | ✅ | `fetch` wrapper; `ApiError` carries the `ProblemDetails.code`; every mutation gets its own `Idempotency-Key` via `crypto.randomUUID()`; `getFloor`/`clearTable`/`requestBill` (FLR-04); `getOrganizations`/`getSites`/`getStaff`/`verifyStaffPin` (WEB-07) |
| `src/api/floorHub.ts` | ✅ | `connectFloorHub(onFloorChanged)` (API-16) — `@microsoft/signalr` client for `FloorHub`, `withCredentials: false` (no cookie auth exists to carry, hard rule 7), reconnects automatically and re-fires `onFloorChanged` on `onreconnected` too so a dropped connection never leaves the floor picker silently stale. A failed initial connect degrades silently — `GET /floor` on every explicit action stays the source of truth regardless |
| `src/api/types.ts` | ✅ | Hand-written mirror of `Brasa.Api/Contracts/*.cs` — kept in sync manually until WEB-03; `OrganizationDto`/`SiteDto`/`StaffDto`/`StaffPinRequest` (WEB-07) |
| `src/components/StaffLogin.tsx` | ✅ | WEB-07 — a header "Sign in" button (disabled until a `siteId` resolves) opening a modal: staff picker (a locked-out member shown but `disabled`, so its PIN can never even be attempted through the UI) → PIN entry → `api.verifyStaffPin`. A wrong PIN clears the field and shows an inline error without leaving the PIN screen; success calls back up to `App.tsx`'s `currentStaff` state, swapping the button for a "Hi, {name}" badge + sign-out. Deliberately does not gate anything else in `pos` — see the feature page's own reasoning for why a real login gate was not attempted here |
| `src/components/TablePicker.tsx` | ✅ | WEB-05 — rooms/tables as a static grid (not `Table.PositionX/Y` — that's the future drag-and-drop editor, FLR-03), colour-coded by state incl. `BillRequested` (its CSS/i18n existed before FLR-04's endpoint did — see the trap below), tap Free to open / tap Dirty to clear. Also hosts "Nova venda ao balcão" (ORD-20), which skips table selection entirely. Groups rooms under a "Floor N" heading (FLR-07) only once `rooms` actually spans more than one `floorLevel` — a single-storey seed renders exactly as before |
| `src/components/ModifierPicker.tsx` | ✅ | CAT-03/04 — shown when a tapped menu item has modifier groups; single-select renders as radio-like buttons, multi-select as toggles capped at `maxSelect`. Validity mirrors the server's own min/max check exactly |
| `src/components/PreBill.tsx` | ✅ | ORD-18/19 — "Ver conta" preview modal. Shaped nothing like `Receipt.tsx`: no document number, ATCUD or QR anywhere in its markup, a bold non-fiscal notice instead |
| `src/components/TransferTablePicker.tsx` | ✅ | ORD-12 — "Transferir mesa" modal listing only currently-`Free` tables; the floor snapshot is re-fetched right before it opens, but the API is still the final word on a race |
| `src/components/*.tsx` | ✅ | `MenuGrid` (renders `Description`/`Allergens` when declared, CAT-02, full contrast — never dimmed, same reasoning as QA-14; renders `imageUrl` as a thumbnail when set, CAT-02, built via a small `apiOrigin` export since the path is relative to the API's own origin, not `/api/v1`; shows `takeawayPrice` instead of `price` when the current order `isTakeaway` and one is set, CAT-06 — the button a waiter taps must show what the order actually charges, not always the dine-in figure), `CouvertBar` (CAT-12 — one-tap strip above `MenuGrid` listing every `isCouvert` item, each button ringing it up at `order.coverCount` in one tap instead of the usual quantity of 1; hidden for takeaway orders, `!order.isTakeaway`, same reasoning as `OrderSummary`'s "Pedir conta" below; renders nothing, not an empty section, when no item is tagged couvert), `OrderSummary` (incl. its own `OrderLineNotes` sub-component, ORD-06 — add/edit/clear a line's kitchen note inline; a +/− quantity stepper per line, ORD-03, disabled at 1 since dropping to zero is what void is for, not an edit; a "Pedir conta" button, FLR-04 — hidden for takeaway orders, `!order.isTakeaway`, since there's no physical table to flag; and a `fire-controls` bar, ORD-07/08 — one button per course that still has an unfired line plus a "Fire all," computed from `order.lines` on every render rather than tracked as separate state, disappearing entirely once nothing is left unfired; a fired line shows a small "Sent" badge next to its name, nested inside the same grid cell as the item name so it doesn't disturb `.order-line-row`'s 3-column layout), `Receipt`, `ErrorBanner`. `LanguageToggle` moved to `@brasa/ui` (WEB-02), imported not local |
| `src/lib/tableLabel.ts` | ✅ | The opposite call from `@brasa/ui`'s `money.ts` on the same axis: renders seeded `"Mesa N"` labels as `"Table N"` in English — display-only, `Table.Label` itself untouched. Unlike money/timestamps, a table label isn't a fiscal-correctness concern, and unlike a menu item's name it isn't identity-bearing content either — floor/kitchen staff who don't read Portuguese still need it. Anything not matching the seeded shape passes through unchanged. Stays local, not shared — `admin` has no table labels to render. See ADR 0011 |
| `src/i18n/i18n.ts` | ✅ | i18next config — pt default, en toggle (WEB-13). Imports `cookieLanguageStore`/`SupportedLanguage` from `@brasa/ui` (WEB-02), not a local file |
| `src/i18n/resources/{pt,en}.ts` | ✅ | UI copy, incl. `floor.*`, `staff.*` (WEB-07). Menu item names and money are **not** here — see the ADR. `error.code.*` is nested to match a server error code's own dots (e.g. `error.code.floor.table_not_dirty`), which i18next resolves with no special config |
| `src/main.tsx` | ✅ | OPS-14 — calls `initErrorReporting()` then wraps `<App />` in `Sentry.ErrorBoundary` (fallback: `ErrorFallback`) plus `DevCrashTrigger`, a dev-only sibling that can throw a real error for E2E to catch |
| `src/lib/errorReporting.ts` | ✅ | OPS-14 — `Sentry.init({ dsn: VITE_SENTRY_DSN \|\| undefined, ... })`, same "empty by default, no real collector yet" shape as OPS-08's OTel wiring; sets `window.__errorReportingInitialized` so E2E can confirm init completed without relying on a Sentry-internal global |
| `src/components/ErrorFallback.tsx` | ✅ | OPS-14 — the `Sentry.ErrorBoundary` fallback: translated "something broke, reload" screen instead of a blank one |
| `src/components/DevCrashTrigger.tsx` | ✅ | OPS-14 — throws for real when `?__crashTest=1` is present, but only inside `if (import.meta.env.DEV && ...)`; a literal `false` in a production build, so dead-code-eliminated there (confirmed by grepping the built bundle) |
| `.env.example` | ✅ | Documents `VITE_API_BASE_URL` and `VITE_SENTRY_DSN` (OPS-14, empty — no real Sentry project exists yet); defaults to the API's `http` launch profile |

⬜ **Missing:** a real auth/session (WEB-07's sign-in is real but
deliberately non-blocking — see its own row), offline (Dexie), everything
else past I0/I1's first slice — see the `WEB` epic in
[backlog.md](../product/backlog.md).

## `src/web/admin` 🚧 WEB-09 shell + WEB-10's menu editor slice

React 19 + Vite 8 + TypeScript, same tooling as `pos`. Both are now npm
workspace members under `src/web/package.json` (WEB-02, added to hoist
`@brasa/ui`'s dependencies to a shared `node_modules` — see that
package's own section), not independent `node_modules` trees. Dev server
pinned to port 5174. No auth yet. "Overview"
and "Menu" are both live; a floor-plan editor (FLR-03) and staff/reporting
screens (WEB-11) are the remaining placeholders.

| File | State | Contents |
|---|---|---|
| `src/App.tsx` | ✅ | Section switcher (`overview`/`menu`/`floor`/`staff`, local state — no router yet) plus "Visão geral"/"Overview": real category/item/room/table counts from `GET /menu/all`/`GET /floor`. No placeholder nav entries left — "Equipa"/"Staff" (IDN-08/09, WEB-11) was the last one. Resolves a `siteId` once on mount (`GET /organizations` → `GET /organizations/{id}/sites`, first of each) for `StaffManager` to key its `GET /sites/{id}/staff` calls by — no site-selector exists anywhere in either client yet, so this is the same single-site shortcut every other screen here already takes, just made explicit since staff genuinely needs a site id to call at all |
| `src/components/FloorManager.tsx` | ✅ | FLR-03 — per room: table list with inline edit (label/seats/shape/position, plain inputs, no drag canvas) and delete (two-step confirm, disabled unless the table is `Free`); an "add table" form per room; a room heading with inline rename and delete (two-step confirm, disabled unless the room has no tables left); an "add room" form. No drag-and-drop canvas for either — that's this row's own still-open gap. Room add/edit also carry a floor-level number input (FLR-07); the room heading shows a "Floor N" badge, but only once `rooms` actually spans more than one `floorLevel` — otherwise every room stays unbadged, same as before. New `siteId` prop (FLR-06) fetches the site's staff list (`api.getStaff`, same call `StaffManager` already makes) and renders a section `<select>` per room (`FloorManagerSection`) — assigning/clearing calls `api.assignRoomSection` and reloads; empty option clears the assignment (`staffId: null`); any staff role appears, not manager-only |
| `src/components/MenuManager.tsx` | ✅ | WEB-10 — per category: visibility toggle; per item: availability toggle, inline price edit, two-step delete confirm, inline takeaway-price add/edit/clear (CAT-06, next to the dine-in price field), inline schedule add/edit/clear (CAT-11 — seven day checkboxes plus native `<input type="time">` start/end fields, whose `"HH:mm"` value needs no conversion to match the wire format), couvert mark/unmark toggle with its own badge (CAT-12, same shape as the availability toggle beside it), photo thumbnail + remove button when `imageUrl` is set, or an upload control when it isn't (CAT-02 — a styled `<label>` wrapping a `display:none` native `<input type="file">`, so the visible click target isn't the plain unstyled input itself). A single import file input (accepts `.csv,.xlsx`, CAT-17) routes to the CSV or Excel endpoint by extension and is the *only* way to add a new item — there's no "create item" endpoint, admin or otherwise. Every mutation refetches `GET /menu/all` rather than reconciling local state by hand |
| `src/components/StaffManager.tsx` | ✅ | IDN-08/09, WEB-11's staff half — list staff (name, role badge, a "Locked" badge when `isLocked`), an "add staff member" form (name/role select/initial PIN), and a per-row "Reset PIN" inline form. No delete/deactivate yet, the same "create + list only" narrow-slice shape IDN-01 itself established. No "verify a PIN" tester here — that's `pos`'s eventual sign-in screen (WEB-07, not built), not a back-office concern |
| `src/api/client.ts`, `src/api/types.ts` | ✅ | `getMenu` calls `GET /menu/all`, not `GET /menu` — see that endpoint's own remarks. Mutating calls now carry `Idempotency-Key`, same as `pos`. Duplicated from `pos`'s own client rather than shared, same reasoning as `pos`'s `api/types.ts` comment: `web/sdk` (WEB-03) is worth building once a change actually needs sharing across clients. `getOrganizations`/`getSites`/`getStaff`/`createStaff`/`setStaffPin` (IDN-08/09) — the first time this app's API layer has ever touched Identity endpoints at all |
| `src/i18n/i18n.ts`, `src/i18n/resources/{pt,en}.ts` | ✅ | Same ADR 0011 shape as `pos`'s `i18n.ts` — deliberately duplicated, not shared: each app's translation resources differ enough that sharing them would trade real duplication for a worse abstraction. `i18n.ts` imports `cookieLanguageStore`/`SupportedLanguage` from `@brasa/ui` (WEB-02) rather than a local `languageStorage.ts` — reuses `pos`'s exact `brasa.lang` cookie name on purpose: it's a preference for the *staff member*, not siloed per client app, so switching language in one carries into the other on the same host. English copy is genuinely English throughout (not just menu-item names left untranslated, which is correct — see the trap in [README.md](README.md)) because not every staff member reading it is a Portuguese speaker |
| `src/main.tsx`, `src/lib/errorReporting.ts`, `src/components/ErrorFallback.tsx`, `src/components/DevCrashTrigger.tsx` | ✅ | OPS-14 — same shape as `pos`'s own (see that app's row for the detail), duplicated rather than shared |
| `.env.example` | ✅ | Documents `VITE_API_BASE_URL` and `VITE_SENTRY_DSN` (OPS-14, empty — no real Sentry project exists yet); previously didn't exist at all |

⬜ **Missing:** auth, the drag-and-drop canvas half of the floor-plan editor (FLR-03 — table/room CRUD itself is done), WEB-11.

## `src/web/e2e` ✅ I0 + I1 harness, plus a first slice of I2

Playwright + TypeScript, chromium only. `playwright.config.ts`'s `webServer`
starts the API (`dotnet run --no-build`), the `pos` dev server and the
`admin` dev server (WEB-09, port 5174) — Docker (PostgreSQL) is the only
thing it doesn't start. Verified locally from both a warm state and a hard
cold start, and **several consecutive full runs** under real 2-worker
parallelism — that repetition is what caught the `Table.Occupy()`
concurrency bug (see the trap in [README.md](README.md)) and then proved the
fix; 74/74 passing on a clean run. At twenty-plus tests, back-to-back full
runs with no pause between them had started occasionally exhausting the
original 8-table pool (QA-02's known limitation showing up in practice, not
a product bug); mitigated by doubling `DevFloorSeeder` to 16 tables — see
e2e-testing.md. See [../development/e2e-testing.md](../development/e2e-testing.md)
for the QA-01 decision record and what QA-04/06/07/08 are still blocked on.

| File | State | Contents |
|---|---|---|
| `tests/walking-skeleton.spec.ts` | ✅ | QA-05 — drives the real `pos` UI: pick a free table off the floor plan → ring up (incl. the modifier picker) → split preview → close → receipt → clear the table. Runs in Portuguese, the app's default |
| `tests/modifiers.spec.ts` | ✅ | CAT-03/04 — the modifier picker itself: required-group validation blocks "Add", Cancel adds nothing, price deltas sum correctly onto the line |
| `tests/pre-bill.spec.ts` | ✅ | ORD-18/19 — no fiscal fields anywhere in the wire shape, VAT-band reconciliation, byte-for-byte matching reprints, 400/409 guards, WCAG scan on the dialog |
| `tests/order-history.spec.ts` | ✅ | ORD-22 — `GET /orders` filtering by status/table, correct totals and line counts, invalid-filter 400s. Also API-09 — walks the full `X-Next-Cursor` chain and checks each of its own created orders appears exactly once, rather than asserting exact page sizes (that flaked under concurrent specs sharing the dev database — see the trap below); a malformed `cursor` 400s |
| `tests/line-notes.spec.ts` | ✅ | ORD-06 — set/overwrite/clear a line's kitchen note via the API, unknown-line/too-long/closed-order guards, and the inline editor in `pos` (add → edit → clear, re-opening starts from the saved value not a stale draft) |
| `tests/discounts.spec.ts` | ✅ | ORD-11 — API-level only (no `pos`/`admin` UI yet). A line discount reducing only that line, an order discount reducing the post-line-discount subtotal further, clearing restoring the original total, and every rejection path (bad type string, out-of-range percentage, an oversized fixed amount, only one of type/value given, an unknown line, a closed order). The two checks that actually matter fiscally: the pre-bill's VAT breakdown still sums to `order.Total`, and `document.GrossTotal` still equals `order.Total` to the cent once the order is closed with a discount applied. Every call goes through the real manager-authorisation gate (IDN-11) via `support/api.ts`'s seeded-demo-manager default — see `tests/manager-authorization.spec.ts` for the gate itself |
| `tests/transfer-table.spec.ts` | ✅ | ORD-12 — old table frees and new table occupies in the same response cycle, the order's lines survive untouched, occupied-target/unknown-table/closed-order guards. UI case uses `transferToAnyFreeTable` (`support/ui.ts`), a second retry-on-409 loop layered on `openAnyFreeTable`'s own — needs `test.setTimeout(120_000)` since their worst cases can add up |
| `tests/transfer-line.spec.ts` | ✅ | ORD-13 — API-level only (no `pos` UI yet). A line moves onto a different order, both totals update and persist, and the guards (same order as destination, unknown line, unknown destination, either order closed) all fire |
| `tests/merge-orders.spec.ts` | ✅ | ORD-14 — API-level only (no `pos` UI yet). Every line moves into the primary order, the secondary ends up empty and `Merged` (persisted), its table frees straight to `Free`, and the guards (self-merge, unknown secondary, either side closed) all fire |
| `tests/split-by-item.spec.ts` | ✅ | ORD-16 — a two-unit line split 1-and-1 across two groups sums back to the cent, the order is unchanged afterward (re-fetched), and the guards (no groups, empty group, unknown line, over-allocation, partial allocation) all fire |
| `tests/split-by-cover.spec.ts` | ✅ | ORD-17 — 22.60 EUR / 5 covers split 2-and-3 → exactly 9.04/13.56, the order is unchanged afterward, and the guards (no cover groups, a zero-cover group, covers not summing to `CoverCount`) all fire |
| `tests/takeaway.spec.ts` | ✅ | ORD-20 — opens with the all-zero table id and a custom or defaulted ("Levantamento") label, closes and issues a real fiscal document exactly like dine-in, and the UI flow ("Nova venda ao balcão", no covers line shown) works end-to-end through a real receipt |
| `tests/menu-item-details.spec.ts` | ✅ | CAT-02 — set/persist/clear a description and allergen set, an unrecognised allergen name 400s, an unknown item 404s, and both render on a real menu button in full contrast (not dimmed) |
| `tests/menu-item-image.spec.ts` | ✅ | CAT-02 — a real uploaded PNG's returned URL is genuinely fetchable through `UseStaticFiles`, not just present in the DTO; replacing a photo deletes the old file from disk (its old URL 404s afterward); an empty file, an oversized (>5MB) file, a disallowed content type and an unknown item are each rejected with their own code; removing a never-set image is a no-op; the `admin` UI uploads and removes a real file through the real browser, confirmed via a follow-up API call |
| `tests/menu-item-course.spec.ts` | ✅ | CAT-14 — API-level only (no admin/`pos` UI yet). Set/persist/clear which course an item is served at, an unrecognised course name and an unknown item both `400`/`404` |
| `tests/menu-item-station.spec.ts` | ✅ | CAT-15 — API-level only (no admin/`pos` UI yet). Set/persist/clear which kitchen station prepares an item, an unrecognised station name and an unknown item both `400`/`404` |
| `tests/void-line.spec.ts` | ✅ | ORD-10 — API-level only (no `pos` UI yet). Voiding zeroes a line's contribution while keeping its snapshot data visible for audit; a voided line disappears from the issued fiscal document but stays on the pre-bill; the reconciliation invariant holds with a void in the mix; missing/blank reason, unknown line, double-void and void-on-closed-order all rejected; voiding every line on an order fails the close at the fiscal layer (`fiscal.no_lines`) rather than the order layer, leaving the order genuinely still `Open`. Every void goes through the real manager-authorisation gate (IDN-11) via `support/api.ts`'s seeded-demo-manager default — see `tests/manager-authorization.spec.ts` for the gate itself |
| `tests/manager-authorization.spec.ts` | ✅ | IDN-11 — the manager-authorisation gate itself. A non-manager staff credential is rejected (`identity.staff_not_manager`) without the void/discount ever touching the line; an unknown `managerStaffId` (`identity.staff_not_found`) and a manager's own wrong PIN (`identity.pin_incorrect`) are both rejected, then the correct PIN still works; the same gate covers line and order discounts, and role is checked before PIN (a non-manager's wrong PIN still surfaces as "not a manager," never "incorrect PIN"); 5 consecutive wrong PINs against a freshly-created isolated manager (not the shared seeded "Ana Ferreira," so parallel specs relying on her stay unaffected) lock them out (`identity.staff_locked`) even for their own correct PIN afterward, mirroring `staff.spec.ts`'s own lockout test |
| `tests/order-course-firing.spec.ts` | ✅ | ORD-07/08/09 — firing a named course sends only its own unfired lines, leaving another course's line untouched; firing all then sends the rest; firing an already-fired course again is a no-op; a voided line is never fired even by "fire all"; a line with no course assigned only ever fires with "fire all," never a named course; an unrecognised course name (`order.invalid_course`), a closed order (`order.not_open`) and an unknown order id (`order.not_found`) are each rejected with their own code; and the `pos` UI fires a course through a real browser, the "Sent" badge appearing and the fire-controls bar disappearing once nothing is left pending |
| `tests/order-line-quantity.spec.ts` | ✅ | ORD-03 — increasing/decreasing a line's quantity recomputes `LineTotal` and the order total to the cent; a percentage discount scales with the new quantity and a fixed one re-clamps if it would now exceed a smaller line's gross; quantity below 1, an unknown line, a voided line (`order.line_voided`) and a closed order all rejected; and the +/− stepper in a real `pos` browser increases/decreases a line and disables "−" at 1 |
| `tests/menu-item-takeaway-price.spec.ts` | ✅ | CAT-06 — sets/persists/clears a separate takeaway price; negative price and unknown item rejected; a takeaway order rings up the takeaway price while a dine-in order for the same item still charges the dine-in one; the `pos` menu button shows whichever price the order in progress would actually charge; the `admin` inline add/edit/clear editor round-trips to the real API |
| `tests/menu-etag.spec.ts` | ✅ | API-10 — `GET /menu` returns `200` with an `ETag` on first pull, `304` with no body (and the same `ETag` restated) when it's echoed back as `If-None-Match`, and an unrecognised `If-None-Match` value still gets the full `200` body. Retries the whole round trip up to 5× — CAT-01/13/19's specs legitimately mutate `GET /menu`'s content (and therefore its ETag) as part of what *they* test, so under real parallel workers a `200` where a `304` was expected can be genuine cross-spec interference, not a broken cache; see the trap below |
| `tests/idempotency.spec.ts` | ✅ | QA-11 — a mutating request replayed 3× with the same `Idempotency-Key` returns byte-identical responses and runs its side effect exactly once: `POST /orders` replayed never creates a second order for the table, `POST /orders/{id}/close` replayed never issues a second fiscal document. Plus the negative cases: a different key against the same now-occupied table is a genuine `409` (not a cache hit), and a missing key `400`s |
| `tests/client-requirements.spec.ts` | ✅ | API-06/07 — a known client id (`X-Brasa-Client: pos-web/0.0.0 (web)`) returns its configured policy; a missing header and a malformed one both `400` the same way (`client.header_required`); a well-formed header naming an unconfigured client id `404`s (`client.unknown_client_id`) |
| `tests/response-compression.spec.ts` | ✅ | API-11 — `GET /menu` comes back `br`-encoded when offered, falls back to `gzip`; a `400` `application/problem+json` error response is compressed too, not just success bodies |
| `tests/menu-import.spec.ts` | ✅ | CAT-17 — a 4-row CSV with 2 valid and 2 invalid rows creates exactly 2 items (confirmed on the real `GET /menu` afterward, not just the import receipt) and reports the other 2 by row number with the bad value named; an empty CSV and a header missing a required column both `400` |
| `tests/menu-import-excel.spec.ts` | ✅ | CAT-17's Excel half — the same 2-valid/2-invalid-row proof as the CSV spec, but as a real `.xlsx` built at test time via `exceljs` (a devDependency of `e2e` only, never a committed binary fixture), plus a wholly-blank row confirmed skipped rather than reported; an empty file, a non-`.xlsx` file, a genuinely corrupt `.xlsx`, and a header missing a required column each `400` with their own code; the `admin` UI imports a real `.xlsx` end to end |
| `tests/menu-availability.spec.ts` | ✅ | CAT-13 — 86'ing a (freshly-imported, isolated) item hides it from `GET /menu` and makes a real `POST .../lines` against it `409` with `catalog.item_unavailable`; un-86'ing restores both; an unknown item `404`s |
| `tests/menu-price.spec.ts` | ✅ | CAT-19 — reprices a (freshly-imported, isolated) item and proves an already-open order's existing line survives unchanged while `GET /menu` shows the new price; negative price and unknown item both `400`/`404` |
| `tests/menu-category-visibility.spec.ts` | ✅ | CAT-01 — hides the real seeded "Sobremesas" category (there's no endpoint to create one, so a live fixture isn't possible) and confirms it and both its items vanish from `GET /menu`; showing it restores both; unknown category `404`s. No longer the only spec touching Sobremesas (WEB-10's own test does too, deliberately — every other category has a real name/item dependency elsewhere), so the id lookup goes through the unfiltered `GET /menu/all` and the whole hide/show/verify round trip retries up to 5×, same shape as `menu-etag.spec.ts`'s fix for the same class of problem |
| `tests/request-bill.spec.ts` | ✅ | FLR-04 — drives a real Chromium browser: open a table, ring up an item, click "Pedir conta", confirm `BillRequested` on the very next `GET /floor`. Plus the API-level guards: a free table `409`s, an unknown table `404`s |
| `tests/accessibility.spec.ts` | ✅ | QA-14 — axe-core against the table picker, ordering screen, modifier picker and receipt (WCAG 2.0/2.1 A+AA). Found 5 real `color-contrast` failures on its first run, all from dimming text via CSS `opacity` — see [status.md](../product/status.md#accessibility-first-scan-five-real-fixes) |
| `tests/admin-shell.spec.ts` | ✅ | WEB-09 — loads `admin` (port 5174) and checks "Visão geral" shows real, non-zero counts from `GET /menu/all`/`GET /floor` (not a static mock); confirms every nav section (incl. "Equipa"/Staff, IDN-08/09) shows no placeholder label, and that clicking into Staff actually renders real seeded data, not just a live-looking label |
| `tests/floor-table-management.spec.ts` | ✅ | FLR-03 — table CRUD: creates/edits/deletes a table via the API, round-tripping through `GET /floor`; an empty label, zero seats, an unrecognised shape and an unknown room all rejected on create; an unknown table 404s on edit/delete; deleting a non-`Free` table 409s (`floor.table_not_free`); the `admin` UI round trip (add → edit → delete) works in a real browser. Room-CRUD follow-up: same shape for rooms — create/rename/delete via the API and the `admin` UI, an empty name rejected on create/rename, an unknown room 404s, deleting a non-empty room 409s (`floor.room_not_empty`) |
| `tests/table-groups.spec.ts` | ✅ | FLR-05 — API-level only, no client UI yet. Grouping 2 free tables makes `POST /orders` 409 (`floor.table_grouped`) against either one; deleting the group restores ordinary seating, proven with a real order open+close, not just a re-read of `GET /floor`. Rejects fewer than 2 tables (`floor.table_group_too_small`), an unknown table (`floor.table_not_found`), a non-`Free` table (`floor.table_not_free`), a table already in another group (`floor.table_already_grouped`), and 404s deleting an unknown group (`floor.table_group_not_found`) |
| `tests/tax-rules.spec.ts` | ✅ | CAT-07/08 — API-level only, not wired into ordering yet. The seeded mainland rates resolve correctly for both bands and both channels; on a region only this spec touches, a later rule supersedes an earlier one exactly within its own window and nothing resolves before the earliest rule's start (`catalog.tax_rule_not_found`); an unrecognised region, an out-of-range percentage, an unparsable date and a backwards effective range are all rejected on create; an uncovered combination 404s on resolve too |
| `tests/floor-multi-level.spec.ts` | ✅ | FLR-07 — `floorLevel` round-trips through create/`GET /floor`/update; omitting it on create defaults to `0`; creating a second-floor room makes `admin`'s "Floor N" badge appear on every room (incl. the seeded ground-floor ones), and editing a room's floor level through the real UI round-trips to the API; `pos`'s table picker renders both floor headings with each room grouped under its own |
| `tests/floor-section-assignment.spec.ts` | ✅ | FLR-06 — assigns a plain Staff-role member (proving the gate isn't manager-only, unlike IDN-11's), resolving the name correctly both on the assignment response and a fresh `GET /floor`; clearing removes both `assignedStaffId`/`assignedStaffName` together; an unknown staff id (`identity.staff_not_found`) and an unknown room (`floor.room_not_found`) both 404; the `admin` UI assigns and clears through the real section `<select>`, each confirmed via a follow-up `GET /floor` rather than trusting the UI alone. Uses isolated rooms (`createRoom`/`deleteRoomResponse`), never the seeded Salão/Esplanada, so parallel specs can't race on the same room |
| `tests/floor-realtime.spec.ts` | ✅ | API-16/17 — two real, separate Playwright browser tabs, neither ever calling `reload()`: opening a table from the second tab flips it to `Occupied` in the first purely from `FloorHub`'s pushed signal; clearing a dirty table flips it back to `Free` the same way. Proves the push itself, not a disguised poll |
| `tests/test-clock.spec.ts` | ✅ | QA-04 — proves the `X-Brasa-Test-Clock` override directly against `GET /ping` (echoes `IClock.UtcNow` verbatim): an override lands only on the request that sent it, a concurrent unheadered request still sees the real clock, and an unparseable header degrades to the real clock rather than erroring |
| `tests/staff.spec.ts` | ✅ | IDN-08/09 — a correct PIN verifies, an incorrect one is rejected (`identity.pin_incorrect`), 5 consecutive failures lock the account so even the *correct* PIN is then refused (`identity.staff_locked`), and resetting the PIN clears the lockout with the new PIN working immediately. An empty name, a malformed PIN, an unrecognised role and unknown site/staff ids are all rejected with their own codes. `admin`'s staff screen adds a staff member and resets their PIN through the real UI, both proven to actually take effect via a follow-up API call, not just "the UI showed no error" |
| `tests/staff-login.spec.ts` | ✅ | WEB-07 — `pos`'s own sign-in: a wrong PIN shows an inline error and leaves the picker on the PIN-entry screen (not the name list) to retry; the correct PIN afterward still succeeds, showing "Hi, {name}" and a sign-out control; signing out returns to the "Sign in" button; cancelling the modal leaves the unsigned-in state untouched. A staff member locked out via 5 API-level wrong attempts appears in the picker but its button is `disabled`, so there's no way to even attempt a PIN against a locked account through the UI. Every fixture is created directly on the real seeded demo site (never an isolated org/site, since `pos` only ever resolves that one site's staff) — the locked-out one is freshly created there, never the shared "Ana Ferreira"/"Tiago Costa" other specs depend on staying usable |
| `tests/admin-language-toggle.spec.ts` | ✅ | WEB-09 — mirrors `pos`'s own `language-toggle.spec.ts`: pt default, switching to `en` translates every nav label and overview card to genuine English (not just some token string) — incl. confirming neither "Floor plan" nor "Staff" show "Coming soon" any more, both live now (FLR-03, IDN-08/09) — and persists in the same `brasa.lang` cookie `pos` uses |
| `tests/admin-menu-management.spec.ts` | ✅ | WEB-10 — API-level: an 86'd item is excluded from `GET /menu` but included (flagged) in `GET /menu/all`, using a freshly-imported/uniquely-named test item (same isolation convention as `menu-availability.spec.ts`/`menu-price.spec.ts`); a category-visibility check is deliberately read-only (every category's `isVisible` must match whether it appears in `GET /menu`, checked against a single snapshot — true regardless of what any concurrent spec is doing to any category at that moment, so it needs no fixture isolation at all). UI-level: 86-ing/repricing/deleting an item and CSV-importing a new one use isolated items; toggling a category's visibility shares the real "Sobremesas" category with `menu-category-visibility.spec.ts` and is written to tolerate that — see the trap in [README.md](README.md) |
| `tests/support/ui.ts` | ✅ | `openAnyFreeTable`/`transferToAnyFreeTable` — retry against a different table on a 409, the UI-side counterpart to `openOrderOnAnyFreeTable` below. Both return the *raw* seeded label (read from `data-testid`, e.g. `"Mesa 6"`), not the displayed text — `src/lib/tableLabel.ts` can render that as `"Table 6"` in English, and every caller uses the return value to build another `data-testid`, not to assert rendered copy. See the concurrency trap in [README.md](README.md) |
| `tests/split-preview.spec.ts` | ✅ | API-level (no browser); sweeps `Money.Allocate` across 1/2/3/5/7-way splits |
| `tests/language-toggle.spec.ts` | ✅ | WEB-13 — default language, the pt→en toggle, cookie attributes (`Path`, `SameSite`, not `httpOnly`) surviving a reload, money staying `pt-PT` in English mode, a seeded table label reading "Table N" (not "Mesa N") on the picker, order heading and receipt alike, and a blank takeaway ticket defaulting to "Takeaway" rather than the API's own "Levantamento" |
| `tests/error-localization.spec.ts` | ✅ | ADR 0011's "Server-sent error text" gap — triggers a genuine `409 catalog.item_unavailable` (86ing a freshly-imported item after the menu already rendered) and asserts the error banner shows the real translated string in both pt and en, not the raw English `ProblemDetails.title` |
| `tests/error-tracking.spec.ts` | ✅ | OPS-14 — `window.__errorReportingInitialized` confirms `Sentry.init()` completes on a normal load in both `pos` and `admin`; a dev-only, build-stripped crash trigger (`?__crashTest=1`) proves `Sentry.ErrorBoundary` catches a genuine thrown error and renders the translated fallback (not a blank screen) in both apps; the fallback passes the same WCAG A/AA axe scan (QA-14) as every other screen |
| `tests/support/api.ts` | ✅ | QA-03 test-data builders. Looks menu items and tables up **by name/state**, never by id (ids are UUIDv7, not stable across a fresh database). `clearTable` returns a table to the free pool directly — 16 are seeded and the dev database persists across runs; `closeOrderAndClearTable` is `closeOrder` + `clearTable` for the common case, factored apart (ORD-11) so a spec that needs the `CloseOrderResponse` itself (e.g. to check `document.GrossTotal`) isn't forced to throw it away. `openOrderOnAnyFreeTable` retries on a 409 — see the concurrency trap in [README.md](README.md). `testClockHeader(instant)` (QA-04) builds the `X-Brasa-Test-Clock` header; `getMenu`/`addLine` both take an optional `headers` param so a test can pass it through without a real wait. `createOrganization`/`createSite`/`createStaff`/`verifyStaffPin`/`setStaffPin` (IDN-08/09). `getDemoManagerCredentials`/`getDemoStaffCredentials` (IDN-11) resolve and cache the seeded demo site's "Ana Ferreira"/"Tiago Costa" staff ids by name; every `setLineDiscountResponse`/`setOrderDiscountResponse`/`voidLineResponse` (and their throwing `*`-without-`Response` counterparts) default to the manager credentials unless a caller passes its own `ManagerCredentials`, so `discounts.spec.ts`/`void-line.spec.ts`/`order-line-quantity.spec.ts`/`split-by-item.spec.ts` needed zero call-site changes when the manager-authorisation gate landed. `assignRoomSection`/`assignRoomSectionResponse` (FLR-06) |
| `load/read-load.mjs`, `load/write-load.mjs`, `load/run-all.mjs` | 🚧 | QA-13 — not Playwright, plain Node scripts (`npm run load`). `read-load.mjs` is `autocannon` against `GET /menu`/`GET /floor`; `write-load.mjs` is a hand-rolled harness since the write path is a correlated multi-step flow autocannon isn't built for — N terminals each claim one seeded table for the whole run and cycle open→add line→close→clear, bounded by the 16-table pool. See [load-testing.md](../development/load-testing.md) for why "50 sites × 5 terminals" (this row's own eventual title) isn't testable yet, and the real ~40x `Debug`-logging latency trap found running this |

## `tests/`

| Project | State | Notes |
|---|---|---|
| `Brasa.Shared.Tests` | ✅ | `Primitives/MoneyTests.cs` — 17 tests, exhaustive allocation over ~12,000 combinations. `ErrorCodeRegistryTests.cs` (API-04) — text-scans every `Error.*(...)` call site under `src/` against `docs/architecture/error-codes.md`, no Docker needed; its own scan regex names all six factories by hand (`Validation|NotFound|Conflict|Forbidden|Failure|RateLimited`), so a seventh would need the regex updated too, not just the enum. `Time/PortugueseTimeZoneTests.cs` — 14 tests: IANA ids resolve on this runtime, Azores stays 1h behind the mainland year-round, the rollover-hour boundary is inclusive, and the same instant can land on two different business days in different regions. `Primitives/ResultTests.cs`/`ErrorTests.cs` — 21 tests: `Value` on a failed `Result<T>` throws with the error code named, `Match`, implicit `T → Result<T>` conversion, each `Error` factory's `ErrorType` incl. `RateLimited` (API-12) |
| `Brasa.Fiscal.Portugal.Tests` | ✅ | 13 tests: `FiscalDocumentLineTests` (gross→net VAT derivation, exhaustive per rate — the regression test for the I0 VAT bug), `MockFiscalProviderTests` (per-tenant sequential numbering, mock markers, mixed-rate reconciliation) |
| `Brasa.Api.IntegrationTests` | ✅ | `TenantIsolationReflectionTests` (DAT-11 — every module's built EF model, no DB) + `TenantIsolationIntegrationTests` (QA-09/10 — real Testcontainers Postgres, queries as `brasa_app` via raw SQL so a disabled RLS policy can't hide behind the EF convenience filter; its `CatalogDbContext` for `MigrateAsync` is built via `CatalogDbContextFactory` itself, not a hand-rolled `DbContextOptionsBuilder` — a hand-rolled copy drifted from the factory in a way that only showed up as `PendingModelChangesWarning` once a real migration existed to validate against, found via CAT-14/CAT-15, see the trap in README.md). `CsvParserTests` (CAT-17, no DB), `ErrorMappingTests` (no DB — pins all 6 `ErrorType`→HTTP status mappings in one place, incl. `RateLimited` → 429), `ApiDeprecationMiddlewareTests` (API-08, no DB — runs the middleware directly against a `DefaultHttpContext`) and `ApiRateLimitingTests` (API-12, no DB — the partition-key logic only, not the real `PartitionedRateLimiter`; that's proven live instead, see status.md) live here too, since this is the only test project referencing `Brasa.Api`. `Mvc.Testing` referenced, not yet used for a full HTTP-level integration test |

## `infra/`

| Path | Purpose |
|---|---|
| `docker-compose.yml` | PostgreSQL 18 (ICU, `pt-PT`) + Seq. Mounts `/var/lib/postgresql`, **not** `.../data`. Mounts `initdb/` for first-run role setup. Seq needs `SEQ_FIRSTRUN_NOAUTHENTICATION` — without it the container crash-loops (a newer `datalust/seq:latest` requires either that or an admin password on first run), reporting `Up` for a few seconds after every restart before dying again, easy to miss without actually querying it |
| `initdb/01-app-role.sql` | Creates `brasa_app` — the unprivileged runtime role. Runs once, on first container init only. See [ADR 0010](../architecture/decisions/0010-rls-runtime-role-split.md) |
| `scripts/backup-database.ps1` | OPS-12 — `pg_dump` (custom format) via `docker exec`, `docker cp`'d out to `infra/backups/` (gitignored). Never a PowerShell text redirect — see the trap in [README.md](README.md) |
| `scripts/restore-database.ps1` | OPS-12 — `pg_restore` into a target database; defaults to a scratch `_restore_drill`-suffixed name, never the real database, unless explicitly overridden |
| `scripts/restore-drill.ps1` | OPS-12 — backup → restore into scratch → compare every table's row count across every schema → PASS/FAIL report → cleanup. The actual "tested" half of this task's own title. See [backup-and-restore.md](../development/backup-and-restore.md) |

## `docs/`

Published to <https://mahbub9.github.io/Brasa/> by `pages.yml`. Directory index
pages are `README.md` so GitHub renders them when browsing a folder; VitePress
rewrites them to site index pages, so one file serves both.

| Path | Purpose |
|---|---|
| `.vitepress/config.mts` | Site config: nav, sidebar, search. **Add new pages to the sidebar or they are unreachable** |
| `README.md` | Documentation index |
| `ai/README.md` | **AI session brief — the entry point for a new session** |
| `ai/repo-map.md` | This file |
| `glossary.md` | Portuguese fiscal and restaurant terminology |
| `architecture/README.md` | System overview: three tiers, ownership, shared kernel |
| `architecture/api-contract.md` | **Rules every endpoint obeys** so Android/iOS ship with no backend change |
| `architecture/money.md` | Why integer cents; allocation, rounding, formatting |
| `architecture/multi-tenancy.md` | Query filters + RLS; the system context |
| `architecture/error-codes.md` | The `Error.Code` registry (API-04) — every code, its `ErrorType`/HTTP status, and what it means. Enforced by `ErrorCodeRegistryTests` |
| `architecture/module-boundaries.md` | The five rules modules obey |
| `architecture/site-agent.md` | In-restaurant process design (stub status) |
| `architecture/conventions.md` | Code conventions, build policy, suppression register |
| `architecture/decisions/README.md` | ADR index with one-line summaries |
| `architecture/decisions/0001..0011` | ADRs — each with a "Revisit when" trigger |
| `fiscal/README.md` | ATCUD, signature chain, QR, SAF-T, document types, VAT |
| `fiscal/certification.md` | AT process, prerequisites, what AT verifies |
| `fiscal/key-management.md` | Signing key custody and open questions |
| `development/getting-started.md` | Prerequisites, build, run, local infra |
| `development/testing.md` | The four-tier testing bar |
| `development/e2e-testing.md` | End-to-end strategy; Playwright recommendation (next session) |
| `development/backup-and-restore.md` | OPS-12 — the backup/restore-drill scripts, why `docker cp` not a PowerShell redirect, live drill results |
| `development/load-testing.md` | QA-13 — read/write load scripts, live results, and the ~40x `Debug`-logging latency trap found running them (see the trap in [README.md](README.md)) |
| `development/documentation.md` | The documentation contract |
| `openapi/README.md` | What `v1.json` is, why `servers` is stripped, how to regenerate it (API-13) |
| `openapi/v1.json` | The `/api/v1` OpenAPI 3.1 document, committed so the API's shape is reviewable in a diff (API-13). Regenerated by hand — no CI drift check yet, that's API-14 |
| `features/` | Per-feature documentation, one page each |
| `product/roadmap.md` | **Increments I0–I8 with demo scripts. What to build next** |
| `product/backlog.md` | 292 tasks, 20 epics, stable IDs. Task status |
| `product/differentiation.md` | Competitive positioning; DIF epic rationale |
| `product/plan.md` | Approved build plan and 6-month roadmap (historical) |
| `product/status.md` | **Honest inventory of which code exists** |

## Not yet created

| Path | Purpose | Increment |
|---|---|---|
| `web/ui` | Shared component library | I0/I1 |
| `web/sdk` | TypeScript client generated from OpenAPI | I0/I1 |
| `web/kds` | Kitchen display | I4 |
| `web/order` | QR self-ordering | I8+ |

Also not yet created: the deployment target for OPS-11 (I0) — the one
remaining piece of I0.
